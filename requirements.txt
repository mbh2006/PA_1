# PA-1 (Beyond IID) - python dependencies
# Training runs on Kaggle (GPU); the list below is also enough for a local
# CPU smoke test of the data pipeline.
torch>=2.1
torchvision>=0.16
numpy>=1.24
Pillow>=10.0
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
PyYAML>=6.0
tqdm>=4.66

# Optional: only needed by shared/prepare_pacs.py to download PACS from the
# HuggingFace hub (https://huggingface.co/datasets/flwrlabs/pacs).
datasets>=2.14

# Optional, used later by Task 1 (cue conflicts / representations):
# open_clip_torch>=2.24
# umap-learn>=0.5
