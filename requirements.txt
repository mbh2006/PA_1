# PA-1 (Beyond IID) - python dependencies.
# On Kaggle, torch/torchvision/numpy/matplotlib are preinstalled; the extras
# needed there are: pyyaml scikit-learn tqdm, plus the Task 1 CLIP stack
# installed WITHOUT dependencies (see README).
torch>=2.1
torchvision>=0.16
numpy>=1.24
Pillow>=10.0
matplotlib>=3.7
scikit-learn>=1.3
PyYAML>=6.0
tqdm>=4.66

# Task 1: CLIP ViT-B/32 backbone (OpenAI weights via OpenCLIP)
open_clip_torch>=2.20

# Optional: only used by shared/prepare_pacs.py to download PACS from the
# HuggingFace hub (https://huggingface.co/datasets/flwrlabs/pacs).
datasets>=2.14
